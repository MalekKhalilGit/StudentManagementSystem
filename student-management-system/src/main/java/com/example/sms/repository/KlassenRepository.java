package com.example.sms.repository;

import com.example.sms.entity.Klasse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KlassenRepository extends JpaRepository<Klasse,Long> {
}
